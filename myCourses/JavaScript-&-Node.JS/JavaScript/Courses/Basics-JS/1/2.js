"use strict";

/*
Необходимо создать переменную "name" и записать в эту переменную своё имя.
Создать переменную "admin", присвоить ей значение из переменной "name".
После чего вывести корректное значение переменной "admin" в консоль.
*/

const userName = "Dayiawan";
const admin = userName;
console.log(admin);
